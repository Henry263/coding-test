# Dell-Secureworks Coding challenge
Angular 7 applicaiton

## How to use
Clone the repository and use `npm install` to install all dependencies.


`ng serve` and go to `http://localhost:4200` to try it out.


